# ================================================
# Clean Hyprland Config - Updated with High Contrast
# ================================================

monitor=,preferred,auto,auto,transform,2

# Environment
env = GDK_DPI_SCALE,0.9
env = QT_SCALE_FACTOR,0.9
env = QT_QPA_PLATFORMTHEME,qt5ct
env = XCURSOR_SIZE,20

# Autostart
exec-once = waybar
exec-once = hyprpaper
exec-once = hyprlock || hyprctl dispatch exit

# Variables
$terminal = alacritty
$fileManager = nautilus
$menu = wofi --show drun --style ~/.config/wofi/style.css

# Screenshots
bind = CTRL SHIFT, P, exec, grim ~/Pictures/$(date +'%Y-%m-%d_%H-%M-%S').png
bind = CTRL SHIFT, S, exec, grim -g "$(slurp)" ~/Pictures/$(date +'%Y-%m-%d_%H-%M-%S').png

input {
    kb_layout = us
    follow_mouse = 1
    touchpad {
        natural_scroll = no
    }
    sensitivity = 0
}

general {
    gaps_in = 0
    gaps_out = 2
    border_size = 1                    # increased for better visibility
    col.active_border = rgba(00ff99ff) rgba(33ccffee) 45deg
    col.inactive_border = rgba(666666cc)
    layout = dwindle
    allow_tearing = false
}

decoration {
    rounding = 2
    rounding_power = 1.0

    blur {
        enabled = true
        size = 3
        passes = 2
        new_optimizations = true
    }

    shadow {
        enabled = true
        range = 8
        render_power = 3
    }

    active_opacity = 1.0
    inactive_opacity = 0.95
    dim_inactive = true          # helps active window stand out more
    dim_strength = 0.05
}

animations {
    enabled = true
    animation = windows, 1, 4, default
    animation = fade, 1, 4, default
    animation = workspaces, 1, 4, default
}

dwindle {
    pseudotile = true
    preserve_split = true
}

master {
    new_status = master
}

gestures {
    gesture = 3, horizontal, workspace
}

misc {
    force_default_wallpaper = -1
    disable_hyprland_logo = false
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more

# Suppress maximize event for all windows
windowrule {
    name = suppress-maximize
    suppress_event = maximize
    match:class = .*
}

windowrule {
    name = no-wofi-anim
    no_anim = on
    match:class = ^(wofi)$
}

# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = SUPER

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, return, exec, $terminal
bind = $mainMod, C, killactive, 
bind = $mainMod, M, exit, 
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, V, togglefloating, 
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, j, movefocus, r
bind = $mainMod,k , movefocus, u
bind = $mainMod, l, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
